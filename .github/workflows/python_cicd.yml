name: Python CI/CD

on:
  push:
    branches:
      - "main"
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"
  pull_request:
    branches:
      - "main"

concurrency:
  group: python-cicd-${{ github.ref }}
  cancel-in-progress: true
